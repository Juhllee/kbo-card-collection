# ☁︎ 구글 시트 동기화 설정 (거래보드)

앱에서 체크한 카드를 본인 구글 드라이브의 시트로 자동 전송합니다.
- **양도가능(보유)** = 내가 가진 카드 (KIA 제외) → 남에게 줄 수 있는 카드
- **구함(KIA 미보유)** = 내가 모으는 KIA 중 아직 없는 카드 → 받고 싶은 카드

## 1회 설정 (약 5분)

1. **구글 시트 만들기**: <https://sheets.new> 접속 → 이름 예: `KBO 카드 거래보드`
2. 상단 메뉴 **확장 프로그램 → Apps Script** 클릭
3. 기본 코드를 모두 지우고 `google-apps-script.gs` 내용을 **전부 붙여넣기**
4. 맨 위 `var TOKEN = 'CHANGE_ME';` 의 `CHANGE_ME` 를 **나만의 비밀키**로 변경 (예: `kbo2026!`) → 💾 저장
5. 우상단 **배포 → 새 배포** 클릭
   - 유형(톱니) → **웹 앱**
   - 설명: 아무거나
   - 실행 계정: **나(본인)**
   - 액세스 권한: **모든 사용자**  ← 꼭 이걸로 (앱이 로그인 없이 보낼 수 있게)
   - **배포** → 권한 검토/허용 (본인 구글 계정 승인)
6. 표시되는 **웹 앱 URL**(`.../exec`로 끝남) 복사

## 앱에 연결

7. 앱 홈 → **☁︎ 구글시트 동기화** → URL과 **비밀키(4번 값)** 입력
8. **저장** → **연결 테스트**(‘연결 OK ✓’) → **지금 동기화**
9. ‘체크할 때 자동 전송’을 켜면 체크할 때마다 몇 초 뒤 자동 반영

## 거래 상대에게 공유 (보기 전용)

10. 구글 시트 우상단 **공유** → ‘링크가 있는 모든 사용자’ = **뷰어** → 링크 복사해서 전달

> 코드를 수정(예: TOKEN 변경)하면 **배포 → 배포 관리 → 편집(연필) → 버전: 새 버전 → 배포**로 갱신하세요.
> URL이 바뀌면 앱 설정에서 다시 입력하면 됩니다.
